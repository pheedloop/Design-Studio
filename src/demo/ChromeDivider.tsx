// Demo chrome stays untranslated on purpose — it is the visual boundary between
// library strings and host strings.

export function ChromeDivider() {
  return <div className="w-px h-4 bg-gray-700 mx-xxxs" />;
}
