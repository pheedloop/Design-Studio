// ---------------------------------------------------------------------------
// Seat plan viewer strings
// ---------------------------------------------------------------------------
//
// Keys must start with `seatviewer.` and may only be referenced from
// src/seatviewer/**. A string shared with another surface goes in
// strings.common.ts instead.
//
// FORMAT (enforced by scripts/verify-strings.ts): entries sorted by key, one
// `"key": "English",` per line, two-space indent.

export const SEATVIEWER_STRINGS = {} as const satisfies Record<string, string>;
